---
trigger: always_on
---

# Types and Interfaces Rule

- All TypeScript interfaces and types for the new `admin-new` flow must be written in TWO consolidated file (e.g., `src/interfaces/interfaces.ts`  `src/types/types.ts`).
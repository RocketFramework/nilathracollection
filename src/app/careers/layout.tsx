import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Careers | Join Our Sri Lanka Tour Company",
};

export default function CareersLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}

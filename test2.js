const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function check() { const { data } = await supabase.from('restaurants').select('id, name, address'); console.log(data.filter(r => (r.address || '').toLowerCase().includes('unawatuna') || (r.name || '').toLowerCase().includes('unawatuna'))); } check();

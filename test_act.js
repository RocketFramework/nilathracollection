const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function check() { const { data } = await supabase.from('activities').select('id, activity_name, location_name, district').ilike('district', '%unawatuna%'); console.log('Activities with District Unawatuna:', data); const { data: d2 } = await supabase.from('activities').select('id, activity_name, location_name, district').ilike('location_name', '%unawatuna%'); console.log('Activities with Location Unawatuna:', d2); } check();

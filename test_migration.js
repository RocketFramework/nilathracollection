const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { error } = await supabase.rpc('run_sql', { sql_query: 'ALTER TABLE daily_activities ADD COLUMN IF NOT EXISTS restaurant_id UUID REFERENCES restaurants(id); ALTER TABLE daily_activities ADD COLUMN IF NOT EXISTS hotel_id UUID REFERENCES hotels(id);' }); console.log(error || 'done'); } run();

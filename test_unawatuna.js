const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function check() { const { data } = await supabase.from('restaurants').select('id, name, address').ilike('address', '%unawatuna%'); console.log('Restaurants:', data); const { data: hData } = await supabase.from('hotels').select('id, name, closest_city, location_address').or('closest_city.ilike.%unawatuna%,location_address.ilike.%unawatuna%'); console.log('Hotels:', hData); const { data: vData } = await supabase.from('vendors').select('id, name, address').ilike('address', '%unawatuna%'); console.log('Vendors:', vData); } check();
